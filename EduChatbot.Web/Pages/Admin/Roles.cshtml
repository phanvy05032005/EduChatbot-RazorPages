@page
@model EduChatbot.Web.Pages.Admin.RolesModel
@{
    ViewData["Title"] = "Role Management";
}

<div class="admin-page">
    <div class="admin-page-header">
        <div>
            <div class="admin-kicker"><span data-i18n="admin.roles.permissions">Access</span> · <span data-i18n="admin.sidebar.roleManagement">Roles</span></div>
            <h1><span data-i18n="admin.roles.title">Role</span> <span class="admin-serif" data-i18n="admin.roles.titleSerif">Management</span></h1>
            <p data-i18n="admin.roles.subtitle">System roles and their corresponding permission scopes.</p>
        </div>
    </div>
    <div class="role-grid">
        @foreach (var role in Model.Roles)
        {
            <section class="admin-card role-card">
                <div class="role-card-top"><span class="role-icon" aria-hidden="true">◇</span><div><strong>@role.Permissions.Count</strong><small>Permissions</small></div></div>
                <h2>@role.RoleName</h2>
                <div class="permission-block">
                    <div class="permission-title">Permissions</div>
                    <ul class="permission-list">
                        @foreach (var permission in role.Permissions)
                        {
                            <li>@permission</li>
                        }
                    </ul>
                </div>
            </section>
        }
    </div>
</div>
